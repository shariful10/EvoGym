import { SelectedPage } from "@/shared/types";

type Props = {
	setSelectedPage: (value: SelectedPage) => void;
};

const Benefits = ({ SelectedPage }: Props) => {
	return <div>Benefits</div>;
};

export default Benefits;
